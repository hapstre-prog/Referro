{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for AI features (network structuring, matching, intro drafting). Optional — app boots with fallback responses when absent. Obtain from Google AI Studio (aistudio.google.com).",
      "requiredAtBoot": false
    },
    {
      "name": "RAPIDAPI_KEY",
      "description": "RapidAPI key for the US Real Estate Listings API (https://rapidapi.com/apimaker/api/us-real-estate-listings). Must subscribe to the API's free tier (Basic = 100 req/month) after getting the key. Powers backend MLS data enrichment for AI recommendations.",
      "requiredAtBoot": false
    }
  ]
}
